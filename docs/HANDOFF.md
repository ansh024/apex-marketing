# Handoff prompt: Apex Marketing location pages

Copy everything below the line into a new chat.

---

I'm continuing work on location pages for Apex Marketing (apex-marketing.ai), a US paid-acquisition
agency. Repo: `ansh024/apex-marketing`, branch `claude/elementor-location-pages-ibffgp`, draft PR #1.
Work through that branch and push there.

## Read these first, in this order

1. `docs/design.md` — the brand design language, extracted from the shipped homepage.
2. `docs/elementor-authoring.md` — how Elementor's atomic (V4) elements actually work, verified
   against the plugin source. §8 is a trap that will bite you; §10 records why the Elementor payload
   is in the format it's in.
3. `design/location-pages/README.md` — the finished Austin design, what the design review changed,
   and the rule for adapting to another city.
4. `elementor-templates/README.md` — the Elementor payload and the two manual steps after import.

## What already exists

- **`design/location-pages/austin-tx.html`** — a finished, verified location page for Austin, TX.
  Zero JavaScript. Serve it with `cd design/location-pages && python3 -m http.server 8000`.
- **`elementor-templates/location-page.json`** — the same page as an Elementor
  `build-composition` payload. Generated by `scripts/build_elementor_location_composition.py`;
  regenerate rather than hand-editing, the script validates ids, XML and class refs and fails the
  build if an em-dash reaches the copy.
- **`docs/design.md`, `docs/elementor-authoring.md`** — the reference docs.

## Please install and use these two skills

They were used for the last design pass and the work is calibrated to them:

```
git clone https://github.com/pbakaus/impeccable.git
cp -r impeccable/.agent/skills/impeccable ~/.claude/skills/

git clone https://github.com/Leonxlnx/taste-skill.git
cp -r taste-skill/skills/taste-skill ~/.claude/skills/
```

`impeccable critique` requires two isolated sub-agents (its hard invariant) and its detector needs
`htmlparser2 css-select css-tree domutils` installed into the skill directory, or it silently
returns a false clean result with exit 0.

## Decisions already made, do not relitigate

- **Elementor Pro is active**, so the end state is ONE dynamic template over a `location` CPT, not
  N duplicated pages.
- **Design target is the "calm, pure-Elementor" version.** The homepage's canvas relief, hero
  sweep, GSAP scroll-pinning and halftone footer do NOT travel. No plugin CSS dependency on
  location pages.
- **Atomic elements first**, falling back to a mature V3/Pro widget where V4 has no equivalent.
  Never an HTML widget full of markup, which is what made an earlier blog template unusable.
- **Exclusivity policy**: no blanket category exclusivity, decided case-by-case on the audit call.
  Confirmed with the business; both the location page and `template-apex-landing.php` now say this.

## Constraints that cost real time to discover

- **Never register a location page as one of the `apex-landing-page` plugin's PHP page templates.**
  `apex-landing-page.php:205-217` dequeues every stylesheet except a five-handle allowlist, which
  would strip Elementor's own CSS. The CPT route is naturally safe because both guards test
  `is_page()`.
- **Atomic elements are gated behind the `e_opt_in_v4` experiment, default off.** Templates using
  them import as empty elements until it is enabled.
- **Global Elementor variables cannot hold a `clamp()`** (a Size variable is a structured
  `{size, unit}` pair). Only flat tokens ported; the fluid type scale stays literal CSS with
  `@media(--breakpoint)` steps.
- **Longhand CSS only** (`padding-top`, not `padding`) and **Elementor breakpoint names only**
  (`@media(--mobile)`, never raw pixel queries). Both fall back to `custom_css`, which Pro 3.35+
  strips.
- **Styles fail soft, settings fail hard.** A malformed style silently produces an unstyled
  element, which is why the payload avoids hand-written `$$type` JSON.
- **Zero em-dashes anywhere visible.** taste-skill §9.G, zero tolerance. Middots max one per line.
- **The copy rule that matters:** a line either says something only true in that city, or says the
  sitewide sentence unchanged. Half-localisation ("built around Austin's highest-value
  neighborhoods") is what reads as machine-written and is the specific thing the last pass fixed.
- **Do not fabricate local proof.** No invented client names, city-specific stats, or "we know this
  market" claims.

## What's left

**Needs a human to check on the live site (I can't reach it from a sandbox):**
1. Elementor version is 4.0.x.
2. The `e_opt_in_v4` experiment is enabled (do it on staging first, it changes the editor sitewide).
3. Whether a staging site exists.
4. Whether the kit already has a Theme Builder header/footer, and whether global class or variable
   labels would collide (import merges by label).

**Build work:**
5. Register the `location` CPT and its fields. Nothing exists yet. Fields sketched in
   `docs/elementor-authoring.md` §7; services, nearby areas and FAQs are repeaters and need a shape
   decision (taxonomy vs ACF repeater vs fixed fields).
6. Get the Austin page into Elementor, either by transcribing from `location-page.json` or by
   submitting it if `elementor/build-composition` has shipped. Two manual steps either way: the
   GoHighLevel booking form as an HTML widget, and optionally the FAQ as a Pro Accordion.
7. Convert the city-specific strings to dynamic tags once the CPT exists.
8. The nearby-area pills are currently plain text, not links, because the sibling pages don't
   exist. They become dynamic links when the CPT ships.

**Open question worth resolving:** nothing in the repo establishes whether Apex has a physical Texas
presence (toll-free number, gmail address, no street address). The page neither claims nor denies
one. If there is a real local presence, saying so is the strongest single addition available.

## What I'd do next

The booking section is the one place the design is still thinner than the homepage's equivalent.
Beyond that, the highest-value move is item 5, the `location` CPT, since it's the only real code
work left and it unblocks 6 through 8.
